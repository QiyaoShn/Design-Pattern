package BetterLogin;

public class UserDAO {	//负责用户表的增删查改，查询用户表
	private  DBUtil db;
	public boolean findUser(String userName,String
	userPassword) {
		db.getConnection();
		return false;
	}
}
